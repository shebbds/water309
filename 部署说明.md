# 二次供水309 — 部署与运维说明

纯静态站点（`workbench/`），电脑/手机公网可访问。
本说明覆盖：**从零部署、访问密码、腾讯云 CloudBase 云同步、高德地图、多设备同步/删除传播、数据替换、常见问题**。

> **本目录是一个全新独立项目**，与原项目 `water-zyf` / `water-all` 完全无关：
> 独立的 GitHub 仓库、独立的 Render 服务、独立的网址、独立的云开发集合。
> 两边代码互不影响，改这边不会动到原项目。

> 安全提示：高德 Key/安全密钥写在 `workbench/app.js` 的 `DEFAULT_SETTINGS` 里，公开仓库会暴露（前端本来就必须用它）。
> 云开发的「环境 ID / Publishable Key」同样可公开；真正的门禁是**访问密码 + 数据库安全规则**。若介意，请把仓库设为私有。
>
> ⚠️ 访问密码是**纯前端门禁**，只能挡住随手打开链接的人，无法抵御查看源码的人。要真正保护数据，请在云开发控制台收紧集合权限。

---

## 0. ✅ 云同步已全部打通（2026-09-17 完成，无需再做任何控制台操作）

**现状**：网页 791 条底档、街道列、密码门禁、地图、云同步、**跨设备编辑**全部验证通过。

| 已完成的事 | 结果 |
|---|---|
| 集合安全规则改为 `{"read": true, "write": true}` | ✅ 用户已在控制台完成 |
| 清理云端历史垃圾文档 | ✅ `已删除 833/833，删不掉 0 条` |
| 重灌准确的底档 | ✅ `791 条 / 畸形文档 0 / 缺 key 0 / key 去重 791` |
| 单设备端到端（拉取 + 编辑 + 回写） | ✅ 真实浏览器验证通过 |
| **跨设备**：设备一改备注 → 设备二看得见 | ✅ 真实浏览器双开验证通过 |

> 如果你**换了云开发环境**或**新建了集合**，那一步仍然要做（见下面的历史说明）：
> 数据库 → 集合 `units` → 权限设置 → 自定义安全规则 → `{"read": true, "write": true}`。
> 直达：`https://tcb.cloud.tencent.com/dev?envId=water309-d8g1c3uy074511212#/db/doc/collection/advantages`
>
> **这一步免费**（安全规则是数据库基础功能，不是会员权益），改完立刻生效。
>
> ⚠️ 代价是**该集合对任何知道环境 ID 的人可读可写**（环境 ID 写在前端，是公开的）。
> 真正的门禁只有访问密码。若不能接受，可改用「仅创建者可读写」，但跨设备编辑会静默失效。
>
> **怎么自查**：进「设置界面」点「测试连接」，显示
> **连接正常：云端「units」集合共 791 条** 即为正常。

### 0.0 为什么这条规则非改不可（两个「静默失败」）

默认规则是「所有用户可读，仅创建者可读写」。不改的话，**别的设备改不动你建的记录**，
而且 SDK **不报错**，只返回 `{"updated":0}` —— 用户在手机上改一条、界面提示「已保存修改」、
云端却一个字没变。同理，删除别人建的文档返回 `{"deleted":0}` 也不报错，
于是清理脚本会谎报「已删除 42/42」而云端纹丝不动。

代码里已用 `mustAffect()` 把这类「不报错但不生效」变成显式错误 + 常驻提示条，
但**规则本身不改，编辑就永远到不了云端**。

### 0.1 为什么不需要买「安全域名」

浏览器直连腾讯云网关（`<env>.<region>.tcb-api.tencentcloudapi.com`）时，网关只在
**Web 安全域名白名单**内的来源上返回 CORS 头；白名单默认只有 `localhost` 等，
线上域名 `water-309.onrender.com` 不在其中 → 浏览器直接拦掉。
而**添加自定义安全域名需要付费套餐**（腾讯云 API 错误码 `OperationDenied.FreePackageDenied`），
所以这条路走不通。

**本项目改用同源反向代理绕开它：**

```
浏览器  ──►  https://water-309.onrender.com/cbapi/...   （同源，不触发 CORS）
                        │
                   Render 静态站 rewrite 规则
                        ▼
            https://<env>.<region>.tcb-api.tencentcloudapi.com/...   （腾讯云网关）
```

- `render.yaml` 里配了 `routes: /cbapi/* → 腾讯云网关`
- `app.js` 的 `patchCloudBaseEndpoint()` 把 SDK 发出的请求地址改道到 `/cbapi/`
  （同时包裹 `fetch` 与 `XMLHttpRequest`，因为 `/auth/*` 认证接口和 `/web` 数据接口是两条独立路径）
- `localhost` 与 `file://` **不改道**（前者本就在默认白名单里，且本地没有这个代理）

实测对照（真实无头 Chrome，2026-09-17）：

| 路径 | 结果 |
|---|---|
| 直连腾讯云网关 | `TypeError: Failed to fetch`（被 CORS 拦死） |
| 走 `/cbapi/` 代理 | `{"data":{"total":833}}` ✅ |

**结论：一分钱不用花。**

---

## 1. 从零部署（首次）

### 1.1 第一步：建 GitHub 空仓库

1. 打开 <https://github.com/new>
2. **Repository name** 填 `water309`（⚠️ **不带连字符**，已按此名创建）
3. 选 **Public**（Render 免费版拉私有库需要授权，公开库最省事）
4. **不要**勾选 `Add a README file` / `.gitignore` / `license`（保持空仓库，否则推送会冲突）
5. 点 `Create repository`，拿到仓库地址 `git@github.com:shebbds/water309.git`

> **仓库名 ≠ 网址**。仓库叫 `water309`，但 Render 服务名（决定网址）叫 `water-309`（带连字符），
> 因为 `water309.onrender.com` 已被原项目的遗留服务占用，详见 1.6 节。

### 1.2 第二步：把本地代码推上去

在本目录 `D:\workbuddy\water-309` 下执行：

```bash
git init -b main
git add .
git commit -m "init: 二次供水309 工作台（CloudBase 同步 + 访问密码门禁）"
git remote add origin git@github.com:shebbds/water309.git
git push -u origin main
```

> **推送统一走 SSH**。本机对 GitHub 的 HTTPS 推送不稳定（代理偶发 502），SSH(22) 稳定。
> 本机公钥在 `C:\Users\13069\.ssh\id_ed25519.pub`，需已加入 GitHub 账号的 SSH keys。

### 1.3 第三步：建 Render 静态站点

Render 控制台 → `New +` → `Static Site` → 连接仓库 `shebbds/water309`：

| 字段 | 填什么 |
|---|---|
| **Name** | `water-309`（**带连字符**；决定网址，全网唯一） |
| **Branch** | `main` |
| **Build Command** | 留空（或 `echo 'static site, no build needed'`） |
| **Publish Directory** | `workbench`（**必须有这一层**，不要填仓库根） |

点 `Create Static Site`，等 1~2 分钟，网址即 **`https://water-309.onrender.com`**。

> 也可用 Blueprint 方式（`New +` → `Blueprint` → 连接仓库），Render 会读 `render.yaml` 自动建。
> 本仓库的 `render.yaml` 里服务名就是 `water-309`，两种方式结果一致。
> **若 Blueprint 报「Blueprint file render.yaml not found on main branch」**，说明代码还没推上 GitHub ——
> 先完成 1.2 的推送，或改用上面的 Static Site 手动方式。
> 注意：Render 静态站的**自定义响应头只能在控制台配置**，`render.yaml` 不支持 `headers` 字段，别往里加。

### 1.4 第四步：配白名单（否则地图空白）

**新网址上线后，只需补高德的白名单**：

| # | 位置 | 加什么 |
|---|---|---|
| ① | 高德开放平台 → 应用管理 → 该 Key → 设置 → **域名白名单** | `water-309.onrender.com`（只填域名，不带 `https://`） |

> 高德 Key 白名单**只能在控制台手动加**，没有 API 可改。
>
> 云开发那边**不用再配安全域名**了——加自定义安全域名要付费套餐，本项目已用同源反向代理 `/cbapi/` 绕开，
> 详见 0.1 节。**换了域名或换了部署平台**时才需要回头重读 0.1 节。

### 1.5 后续发布流程

改完代码 → `git push origin main` → Render 自动重部署（约 1 分钟）。

> **⚠️ 改前端后必做**：`index.html` 底部 `<script src="xxx.js?v=YYYYMMDDx">` 的版本号要一起改。
> 线上 HTML/JS 走 CDN 边缘缓存，不改版本号用户会看到旧页面。用户侧需 `Ctrl+Shift+R` 强刷。
>
> **⚠️ 更要紧的一点：光改版本号还不够，因为 `index.html` 本身也被缓存了。**
> Cloudflare 给 HTML 设了 `s-maxage=300`，也就是说**部署后最长 5 分钟内，
> 直接打开域名仍可能拿到旧的 index.html**——而旧 HTML 里引用的 JS 版本号也是旧的，
> 于是整站都停在上一版，看起来就像「push 了但没生效」。
>
> 这段时间内验证线上千万别直接开域名，要么加个随机参数，要么关掉缓存：
> ```bash
> curl -s "https://water-309.onrender.com/index.html?nc=$(date +%s)" | grep -o 'app.js?v=[0-9a-z]*'
> ```
> 用无头浏览器验证时，记得 `Network.setCacheDisabled({cacheDisabled:true})`，
> 否则**验证脚本会对着上一版做断言**，得出的结论全是错的（本项目真实踩过）。
>
> **用户报「改完没生效」时的第一手段**：先确认线上文件时间与版本号，再决定是改代码还是让用户刷新：
> ```bash
> curl -sI https://water-309.onrender.com/index.html | grep -i "last-modified\|cf-cache-status"
> curl -s "https://water-309.onrender.com/index.html?nc=$(date +%s)" | grep -o 'app.js?v=[0-9a-z]*'
> ```

### 1.6 ⚠️ 名字坑：`water309` 与 `water-309` 不是同一个

这两个域名长得很像，但指向完全不同的东西，**别搞混**：

| 域名 | 归属 | 说明 |
|---|---|---|
| `water309.onrender.com`（**无连字符**） | ❌ **原项目的遗留服务** | 早先试验时建的 Render 服务，连的还是老仓库 `shebbds/water-zyf`，线上跑的是旧版页面（脚本 `?v=20260914d`，无 `gate.js`）。**本项目不用它，也不要动它。** 若确认不再需要，可在 Render 控制台手动 Suspend 或 Delete。 |
| `water-309.onrender.com`（**有连字符**） | ✅ **本项目** | 本项目要用的地址，2026-09-17 检查时是空闲的（未占用）。 |

> 同理，**GitHub 仓库名是 `shebbds/water309`（无连字符）**，Render 服务名是 `water-309`（有连字符）。
> 仓库名不影响网址，网址只由 Render 的服务名决定。

---

## 2. 访问密码（门禁）

- **密码：`shui309`**（由 `workbench/gate.js` 校验）
- **锁屏界面**只有标题、密码框、「进入」按钮，以及出错/超时时的状态提示（说明性文字已按要求全部删除）。

### 防护规则（2026-09-19 加强）

| # | 规则 | 参数（`gate.js` 顶部） |
|---|---|---|
| ① | 密码用 **PBKDF2-SHA256** 校验（16 字节盐 + 12 万轮迭代），代码里不落明文 | `KDF_SALT` / `KDF_HASH` / `KDF_ITER` |
| ② | 连续输错 **5 次** → 冷却 **60 秒**，期间输入框禁用；失败计数存 `localStorage`，**刷新页面绕不过去** | `MAX_FAIL` / `COOL_MS` |
| ③ | 无操作 **10 分钟** 自动重新锁定 | `IDLE_MS` |
| ④ | 切到后台/最小化超过 **1 分钟**，回到前台立即锁定 | `AWAY_MS` |
| ⑤ | 解锁状态只存 **`sessionStorage`**：关闭标签页/浏览器后必须重新输密码 | `SS_ACT` |

- 浏览器不支持 `crypto.subtle`（非 HTTPS / `file://`）时，退化为**迭代 djb2**（6 万轮）校验，
  对应 `FB_SALT` / `FB_HASH` / `FB_ITER`。
- 门禁在 `app.js` 之前加载；未解锁时应用完全不启动（不拉云端数据、不建地图），
  所以只看源码也拿不到内存里的数据。

> ⚠️ 这仍然是**纯前端门禁**，只能挡住随手打开链接的人，无法抵御能改内存/下断点的人。
> 真正的数据保护靠云开发集合的安全规则（见第 0 节）。
>
> ⚠️ ⑤ 的副作用：**同一个浏览器里新开一个标签页也要重新输密码**（`sessionStorage` 按标签页隔离）。
> 这是「关闭标签即失效」的必然代价；若觉得太频繁，把 `SS_ACT` 改回 `localStorage` 即可。

### 改密码

1. 生成新的 PBKDF2 与兜底校验值（**改完务必自测能否登录**）：

```bash
node -e "
const {webcrypto}=require('crypto');
const SALT='b7f3c1a95e2d48f60a1c7b3e9d5f2846', ITER=120000, PW='新密码';
(async()=>{
  const k=await webcrypto.subtle.importKey('raw',new TextEncoder().encode(PW),'PBKDF2',false,['deriveBits']);
  const b=await webcrypto.subtle.deriveBits({name:'PBKDF2',salt:Buffer.from(SALT,'hex'),iterations:ITER,hash:'SHA-256'},k,256);
  console.log('KDF_HASH =',Buffer.from(b).toString('hex'));
  const FB='w309-gate-v2', FI=60000; let h=5381,s=FB+'\u0001'+PW;
  for(let r=0;r<FI;r++) for(let i=0;i<s.length;i++) h=(((h<<5)+h)+s.charCodeAt(i))>>>0;
  console.log('FB_HASH  =',h.toString(16));
})();"
```

2. 把 `workbench/gate.js` 里的 `KDF_HASH` 与 `FB_HASH` 换成新值（换盐则同时改 `KDF_SALT`）。
3. 同步改 `index.html` 底部的脚本版本号，push 并让用户强刷。

> 回归测试 `smoke.js`【1】会用同样参数在 Node 侧复算一遍并断言与 `KDF_HASH` 一致 ——
> 常量写错等于把所有人锁在门外，这一步不能省。

### 手动立即锁定

浏览器控制台执行 `sessionStorage.removeItem("sp_shop_gate_act")` 后刷新即可。

---

## 3. 腾讯云 CloudBase 云同步配置

数据存在云开发的**文档型数据库集合**里（默认集合名 `units`），实现多设备共享与实时同步。
配置写在 `workbench/app.js` 的 `DEFAULT_SETTINGS`（部署版生效），也可在网页「设置界面」临时改（只存当前浏览器）。

### 3.1 首次使用：控制台三步（缺一不可）

> 原先是「四步」，其中「配安全域名」这一步**本项目已不需要**（见 0.1 与下表 ②），所以实际只剩三步。

| 步骤 | 位置 | 做什么 |
|---|---|---|
| ① 建环境 | [云开发平台](https://tcb.cloud.tencent.com/) | 新建环境，记下**环境 ID**（形如 `xxx-1a2b3c4d`）。地域默认上海。<br>**⚠️ 数据库类型必须选「云数据库」（文档型），不要选「PostgreSQL 数据库」**——前端 `app.database()` 只连文档型；PostgreSQL 要走 `app.models`/PG 直连，选了连不上 |
| ② ~~配安全域名~~ **本项目已不需要** | — | **不要在这里花时间**。加自定义安全域名要付费套餐（见 0.1），本项目已用同源反向代理 `/cbapi/` 绕开。除非你换了域名/换了部署平台，才需要回头看 0.1 节 |
| ③ 开匿名登录 | [控制台 → 身份认证 → 登录授权](https://tcb.cloud.tencent.com/dev?envId=#/identity/login-manage) | 开启**匿名登录**。<br>**⚠️ 新版环境默认是「未开启」，不是自动开好的**——不开的话 SDK 能加载、连接能建立，但**所有读写全部失败**，顶栏一直显示「实时同步：未连接」，控制台报 `登录方式未开启` 和 `Cannot read properties of null (reading 'scope')`。**本项目实测就卡在这一步。** |
| ④ 建集合 + 放权限 | 文档型数据库 → 集合管理 | **新建集合 `units`**（名字要和「设置界面 → 集合名」一致）；「权限管理」切到**安全规则**，填下面这段 |

> ⚠️ **第 ④ 步的「建集合」经常被跳过，后果非常隐蔽**：
> 集合不存在时，SDK 的读写调用**不会抛异常**（见 3.4 的坑），
> 页面会显示「同步成功 / 已与云端对齐」，但云端一条都没写进去，
> 「测试连接」还会显示「连接正常：云端共 0 条」。
> **先建集合，再谈同步。**

> 本新项目建议**用一个独立的云开发环境**（或至少独立集合名，如 `units309`），
> 避免与原项目共用集合导致两边数据互相覆盖。若共用同一环境，请把「设置界面 → 集合名」改成 `units309`。

安全规则（允许所有访问者读写；本工具靠前端密码把门，没有多租户需求）：

```json
{
  "read": true,
  "write": true
}
```

> 想更严格：把 `write` 改成 `"auth != null"`，仍然允许匿名登录用户写入，但拒绝未登录请求。

### 3.2 在网页里填配置

「设置界面 → 腾讯云 CloudBase 云同步配置」：

- **环境 ID**：必填（如 `myenv-1a2b3c4d`）
- **地域**：必须与环境所在地域一致，选错连不上
- **Publishable Key**：可留空
- **集合名**：默认 `units`（本新项目建议填 `units309`）
- **多设备实时同步**：默认开启

填好后点「测试连接」，提示「连接正常：云端共 N 条」即通。然后点「保存设置」。

### 3.3 数据字段

集合里每条文档：

```
key(业务主键=卫生许可证号) / id / name / address / street / license /
valid_from / valid_to / lng / lat / remark / device_type / contact / updated_at
```

`key` 是同步用的唯一标识；`_id` 由云开发自动生成。

**`street`（街道）是 2026-09-17 新增的一等字段**，来源是底档里的「街道」列（如「和平里街道」）。
它不是摆设，全链路都通了：台账多一列、搜索能搜到、新增/编辑表单能填、详情弹窗显示、
目标卡片显示、Excel 导入导出都带。

> ⚠️ 云集合 `units` 是**无模式**的，新增 `street` 不需要去控制台改表结构，
> 但**旧文档不会自动补上这个列**——只有本端重新上传（或编辑过该条）才会写进去。

### 3.4 排错

- **`CloudBase SDK 加载失败`**：两个 CDN 都不通，换网络重试。
  ⚠️ **不要改成 CDN 的 ESM 版（`+esm`）**——`@cloudbase/js-sdk` 的 ESM 包依赖 `text-encoding-shim`，
  而 CDN 转换后的该模块不导出 `TextDecoder`，`import()` 会直接抛
  `does not provide an export named 'TextDecoder'`，SDK 完全加载不进来。
  必须用官方 UMD 全量包 `https://static.cloudbase.net/cloudbase-js-sdk/2.28.8/cloudbase.full.js`（代码里已配好）。
- **CORS / 跨域报错（`TypeError: Failed to fetch`）**：**先确认 `/cbapi/` 代理还在**。
  本项目不依赖安全域名白名单，而是靠 `render.yaml` 的 rewrite 把 `/cbapi/*` 转到腾讯云网关（见 0.1）。
  所以排查顺序是：

  ```bash
  # ① 代理通不通：只要返回**腾讯云的真实 JSON** 就算通
  #    （不带 token 时正常会返回 PERMISSION_DENIED / Unauthenticated access is denied，这是对的）
  #    报 CORS / Failed to fetch / 404 才是代理坏了
  curl -s "https://water-309.onrender.com/cbapi/web?env=water309-d8g1c3uy074511212" \
    -H "Origin: https://water-309.onrender.com" -H "Content-Type: application/json" \
    -d '{"action":"database.queryDocument","dataVersion":"2020-01-10","env":"water309-d8g1c3uy074511212","collectionName":"units","query":"{}","limit":1}' | head -c 300
  # 实测输出：{"code":"PERMISSION_DENIED","message":"Unauthenticated access is denied ..."}  ← 代理正常

  # ② 直连腾讯云网关做对照：应当缺 access-control-allow-origin（说明确实必须走代理）
  curl -s -i -X OPTIONS "https://water309-d8g1c3uy074511212.ap-shanghai.tcb-api.tencentcloudapi.com/web?env=water309-d8g1c3uy074511212" \
    -H "Origin: https://water-309.onrender.com" -H "Access-Control-Request-Method: POST" | grep -i access-control
  ```

  ① 返回真实 JSON → 代理正常，问题在别处（多半是匿名登录或集合权限，见下面两条）。
  ① 报错 → 检查 `render.yaml` 的 `routes` 是否被改动、Render 是否同步成功。
- **历史坑（已不再适用，留作背景）**：以前靠安全域名白名单，而白名单**只作用于数据库接口 `/web`**，
  认证接口 `/auth` 是全局放行的，于是会出现「匿名登录成功（顶栏显示已连接）但读写全失败」这种极迷惑的现象。
  现在改走 `/cbapi/` 同源代理后**不存在这个问题**；`app.js` 的 `patchCloudBaseEndpoint()` 同时包裹了
  `fetch` 与 `XMLHttpRequest`，两条路径一起改道（只改 `/web` 会重现上面那个现象）。
- **建环境时选错数据库类型**：必须选**云数据库**（文档型）。选成 PostgreSQL 的话前端 `app.database()` 连不上，需要另建环境。
- **`匿名登录失败` / 控制台报「登录方式未开启」**：控制台没开匿名登录，见 3.1 第 ③ 步。
  **这是最常见的坑**：表现是能加载 SDK、能建连接，但**所有读写都失败**，
  控制台报 `Cannot read properties of null (reading 'scope')`，顶栏一直显示「实时同步：未连接（兜底轮询中）」。
- **权限拒绝 / DATABASE_PERMISSION_DENIED**：集合安全规则没按 3.1 第 ④ 步设置。
- **`DATABASE_COLLECTION_NOT_EXIST`**：集合还没建，或名字和「设置界面 → 集合名」不一致。见 3.1 第 ④ 步。
- **「云同步：未配置」**：环境 ID 没填或没保存。
- **⚠️⚠️ 最阴的一个坑：SDK 报错时是 resolve 而不是 reject**

  CloudBase JS SDK 2.28.8 在接口层报错时，`await col.get()` **不会抛异常**，
  而是安静地返回一个 `{code, message, requestId}` 对象。如果代码只写 `try/catch`，
  就会把这些错误全部当成「成功但返回空」：

  | 场景 | 错误处理没做对时的表现 |
  |---|---|
  | 集合不存在 | 「测试连接」显示**连接正常：云端共 0 条**；同步提示**成功**，实际一条没写 |
  | 无写权限 | 新增/导入后提示保存成功，刷新后数据消失 |
  | 参数非法 | 静默失败，无任何提示 |

  代码里的应对是 `unwrap()` 函数（`app.js` 云同步段），**所有云调用都必须过这一层**：

  ```js
  function unwrap(res, what){
    if(res && typeof res === "object" && res.code){
      var e = new Error((what ? what + "：" : "") + (res.message || res.code));
      e.code = res.code;
      throw e;                      // 把「假成功」变成真异常
    }
    return res;
  }
  // 用法：unwrap(await col.add(doc), "写入云端失败")
  ```

  > 改云同步代码时，**新增任何一个 `col.xxx()` 调用都要包 `unwrap`**，
  > 否则就会重新引入「谎报成功」。回归测试 `smoke.js` 的【5】节专门守着这一点。

### 3.5 怎么判断云同步到底通没通（别被假象骗了）

顶栏徽标只是参考，**真正的判据是数据能不能跨浏览器回来**：

1. 在页面里新增一条记录（随便填个单位名）。
2. 浏览器控制台执行：
   ```js
   localStorage.removeItem("sp_shop_data_v2");
   localStorage.removeItem("sp_shop_synced_v2");
   ```
3. 刷新页面。**如果那条记录又出现了，说明它确实写进了云端**；如果没了，说明只存在本地。

> ⚠️ 常见误区：新增后**直接刷新**看到记录还在，**不能**证明云同步成功——
> 那条记录本来就写在 localStorage 里，刷新后自然会读回来。
> 必须先清掉本地缓存再刷新，才有说服力。

浏览器控制台还可用这两句快速自查：

```js
typeof window.cloudbase            // "object" = SDK 已加载；"undefined" = SDK 没加载进来
JSON.parse(localStorage.getItem("sp_shop_settings_v1")).cbEnv   // 应等于你的环境 ID
```

---

## 4. 高德地图配置（地图不显示几乎都是这里）

网页端地图用高德 JS API 2.0。Key 写在 `app.js` 的 `DEFAULT_SETTINGS`：`amapKey` / `amapSecurity`。

**当前内置的 Key**（2026-09-17 更换）：

```
amapKey      = d18a8dd2e58b05ab460f0eaa71eee15e
amapSecurity = 348b837bda1afacb88187f829ccf52d3
```

**最常见故障：地图整片空白/灰、或控制台报 `INVALID_USER_SCODE` / `USERKEY_PLAT_NOMATCH`**
→ 高德 Key 的**域名白名单**没放行当前域名。

修复（高德开放平台 → 应用管理 → 找到该 Key → 设置 → 域名白名单）：
- 加 `water-309.onrender.com`；只填域名，不带 `https://`
- 本地调试顺手加 `localhost` 和 `127.0.0.1`
- Key 类型须是 **Web端(JS API)**；如设了白名单则必须包含上述域名，否则被拒

### 4.1 ⚠️ 换高德 Key 的完整步骤（漏一步就会「改了没生效」）

首次运行时内置配置会被**固化进浏览器 localStorage**，之后每次打开都是本地值优先。
所以只改 `app.js` 里的 Key 而不升版本号，老浏览器会继续用旧 Key，表现为**地图一直空白**。

正确步骤（三步缺一不可）：

1. 改 `app.js` 的 `DEFAULT_SETTINGS.amapKey` / `amapSecurity`
2. **把同文件的 `SETTINGS_REV` 加 1**（当前为 `2`）
   —— 升版后高德 Key/密钥强制以内置值为准，而用户自己填的云环境 ID 等配置仍会保留
3. 升 `index.html` 底部三个 `<script src="xxx.js?v=...">` 的版本号，再 push

> 临时绕过（不推荐）：让用户在浏览器控制台执行
> `localStorage.removeItem("sp_shop_settings_v1")` 后强刷，会重新固化内置值。

---

## 5. 多设备同步 / 删除传播（重点）

同步模型：**云端集合为准**。

- 台账里任何操作（新增/导入/编辑/办结/删除）后**立即**与云端全量对齐（`syncNow`）：
  拉取云端全量 → 逐条比对 → **缺的新增、变的更新、多的删除**。只有真正变化的文档才会写云端，不会全量重写。
- 页面打开（解锁后）自动从云端**合并**一次（`pullCloud({merge:true})`）。
- 合并逻辑：先合并云端记录进本地（新增/修改），再删除「本地有、但云端已无」的记录。
  ⇒ 在某设备删除单位 → 云端少了该记录 → 其它设备自动把自己本地那份也删掉。**删除会在设备间传播。**
- 顶栏徽标显示实时通道状态：`等待首次同步完成…` / `连接中…` / `已连接` / `未连接（兜底轮询中）`。

### 5.1 实时同步与兜底轮询

- **实时同步**：订阅集合变更（`collection.watch`），任意设备增删改，其它已打开的页面约 1 秒内自动更新。
- **兜底轮询**：实时通道不可用（或关闭）时，每 **45 秒**静默拉取一次；页面从后台切回前台时也会立即拉一次。
  这样即使实时推送没连上，多设备也能在 1 分钟内收敛。

#### 徽标文案的含义（按出现顺序）

| 文案 | 含义 | 要不要管 |
|---|---|---|
| `等待首次同步完成…` | 刚解锁，正在拉云端全量 + 对齐；**完成后才会开实时通道** | 不用，十几秒内会变（791 条约需 10~20 秒） |
| `连接中…` | 正在建立实时通道 | 不用 |
| `已连接` | 一切正常 | — |
| `未连接（兜底轮询中）` | 实时通道断了，已降级为 45 秒轮询 | **数据不受影响**；正常会自己连回来，详见下 |
| `已关闭` | 设置界面里关掉了实时同步 | 按需开启 |
| `云同步：未配置` | 环境 ID 没填 | 去设置界面填 |

#### ⚠️ 为什么「未连接」可能一直不恢复（2026-09-19 修复）

SDK 内部的实时通道用的是**有限次数的重连凭据**——实测日志里会打印
`[realtime] INIT_WATCH use a retry ticket, now only N retry left`，
**用尽之后 SDK 自己不再重连**。而一个页面可能一开就是几小时、期间经历多次切网 / 笔记本合盖休眠，
凭据一旦耗光，徽标就会**永久停在「未连接（兜底轮询中）」直到手动刷新页面**。

修复（`app.js`）：

- 掉线后**由 app 自己安排重连**，不依赖 SDK 的有限凭据：退避 `30s → 60s → 120s → 300s`（封顶）。
- **切回前台时立刻重连一次**，不等退避窗口（笔记本合盖再打开的场景）。
- 后台标签页不重连（定时器会被浏览器节流，省电）。
- 兜底轮询期间**数据始终保持同步**，所以即使实时通道一直没连上，**也不会丢数据、不会漏改动**，
  只是「别的设备的改动」从约 1 秒变成最多 45 秒可见。

> 这段逻辑的证据在实测日志里：强制断网后出现 `initWebSocketConnection connect fail`
> → 恢复网络后 `ws event: open` → `initWatch success`，但期间确实消耗了一张重连凭据。

**已知权衡（无大碍）**：因为「云端为准」，若在**手机离线**时新增了一条且从没推上云端，下次联网拉取会被清掉（云端没有它）。主要在电脑在线维护则碰不到。

### 5.2 ⚠️ 两个「静默丢数据」的时序坑（2026-09-17 修复，改云同步代码前必读）

这两个坑的共同点：**全程没有任何报错**，界面还会显示「已保存修改」「已与云端对齐」，
但数据实际上丢了。它们的成因都与「网络等待」有关。

#### 坑 A：刚保存的编辑被并发的云端拉取抹掉

**现象**：改一条记录的备注 → 提示「已保存修改」→ 几秒后备注自己变回原样，云端也没收到。

**成因**：拉取（`pullCloud`）的结构是「读云端全量快照 → 合并进 `state.data`」，
中间隔着十几秒网络等待。如果这段等待里用户点了保存，
拉取手里那份**编辑之前**的快照合并回去就把改动盖掉了。
随后全量对齐（`syncNow`）读到云端旧值、与**已被盖掉的**本地值比较「一致」→ 连写都不写。

**修法**：新增 `state.rev`（本地修订号，任何本地改动 +1）。
拉取进入时记下 `rev0`，读完快照后若 `rev` 变了 → **整份丢弃**（不合并不剪枝不落盘），
并安排一次用新快照的重试。另外：全量对齐进行中时，合并式拉取一律让路。

#### 坑 B：全量对齐把别的设备新增的记录删掉

**现象**：本机几天没打开，这期间别的设备新增了几条；本机重新打开后随手改一条记录，
对方新增的那几条就从云端消失了。

**成因**：全量对齐的删除判据原本是「云端有、本地没有 → 删掉」。
本机离线期间别的设备新增的记录，本机**从没见过**，却被判定为「用户已删除」。

**修法**：判据收紧为 `云端有 && 本地没有 && 本机曾确认过它（synced）`，
即只有「本机确实曾经有过、现在没了」才算用户主动删除。删除传播不受影响。

> **回归测试**：`smoke.js` 159 项，其中【11】用「慢云」放大网络等待精确复现坑 A，
> 【12】复现坑 B，两条都验证过「去掉修复即变红」。跑法见 8.3。

---

## 6. 数据来源、替换与清空

页面数据 = 本地 `localStorage`（键 `sp_shop_data_v2`）+ 云端集合 `units`。

### 6.1 首装种子数据（`seed.js`）

`workbench/seed.js` 里存着 **791 条东城区二次供水单位底档**（数据源：`水科底档9.16-已处理.xls`）。
它的作用只有一个：**让一台从没打开过本页面的新设备，一进去就有完整数据**。

触发条件是**「从未保存过本地数据」**（`localStorage` 里没有 `sp_shop_data_v2` 这个键）：

1. 新设备打开页面 → `loadData()` 发现没有本地数据 → 载入 791 条种子 → 立即写入本地；
2. `init()` 拉完云端后做一次**补推**：只要本地还有「从未在云端确认过」的记录，
   就执行一次全量对齐把种子上云；
3. 此后该设备再打开，所有记录都已在云端确认过，**不会再灌种子、也不会重复推送**。

> ⚠️ 关键点：种子**只在首装时载入一次**，而且推送用的是「全量对齐」——
> 只会补齐云端缺的、更新云端不同的，**不会删除云端任何文档**。
> 所以对一台已有数据的老设备，种子不会造成任何破坏。

> ⚠️ 另一个关键点：用户主动**删空**后（`sp_shop_data_v2` = `"[]"`），种子**不会复活**。
> 判断依据是「键存不存在」，不是「数组长不长」。这是刻意设计的，否则永远删不干净。

> 💡 **补推机制也是「同步失败」的救星**：如果某次同步因为域名没放行 / 集合没建 / 断网而失败，
> 那些只存在本地的记录**下次打开页面会自动补推上去**，不需要重新导入。
> 判据是「这条记录有没有被标记为『云端已确认』」，与是不是种子无关。

**改种子数据**：直接编辑 `seed.js` 的数组即可。改完记得升 `index.html` 里的脚本版本号
（`seed.js?v=...`），否则 Cloudflare 边缘缓存会让用户拿到旧文件。

### 6.2 清空全部数据（本地 + 云端）

1. 云端：「设置界面 → 清空云端数据」（会二次确认）。
2. 本地：浏览器控制台执行 `localStorage.clear()` 后刷新；或改 `app.js` 里 `LS_DATA` 的版本号（`v2`→`v3`）并 push。

> 旧版本的 `hp_workbench_*`、`sp_shop_*_v1` 键会在启动时自动清理，无需手工处理。

### 6.3 导入新底档

直接在「单位台账 → 导入 Excel / 表格」上传即可，导入后立即落盘并同步到云端。

表头需含：编号 / 单位名称 / 经营地址 / 卫生许可证号 / 有效期始 / 有效期止；
可选列：**街道** / 设备类型 / 联系人 / 备注。

> 「街道」列也兼容写成「所属街道」。同一条记录若已存在（按卫生许可证号判定），
> 导入时**会补写街道字段**而不会重复新增一条。

---

## 7. 地图界面交互约定（改这块代码前先读）

- 地图对象必须带 `doubleClickZoom:false`（双击留给「查看单位详情」）。
- **单击标记 = 设为目标**（260ms 延迟，等双击判定）；**双击标记 = 弹单位详情且不切换目标**（DOM `dblclick` + SDK `dblclick` + 时间戳检测三路兜底）。
- **取消目标/距离圆**：点地图空白、点距离圈（`AMap.Circle` 的 click 不冒泡到地图，必须单独绑）、面板「清除目标」按钮、Esc 键。
- **周围单位列表**：单击 = 该单位标记变橙 + 跳动 2 秒后自动恢复（不换目标）；双击 = 切换为目标单位。
- **距离圆只画一个**：`drawTargetCircles(activeRadius)` 只画当前选定/自定义的那一个圆。
- **未编码单位不上地图**：`placeMarkers()` 开头 `if(rec.lng==null || rec.lat==null) return;`，
  没有坐标的记录**不会**在地图上打点。这是刻意保留的行为（见 8.2）。

---

## 8. 首页「已过期单位」区块 与 未编码单位手动定位（2026-09-19 新增）

### 8.1 首页「已过期单位」（独立区块）

位置：首页「到期提醒」卡片**下方**，是一个独立的 `.card`（`#overdue-card`）。

| 元素 | 说明 |
|---|---|
| `#overdue-count` | 红色徽标，显示全部已过期单位数量 |
| `#overdue-search` | 搜索（单位 / 街道 / 地址 / 许可证号），150ms 防抖 |
| `#overdue-export` | 「导出已过期」→ `exportRowsToXlsx(list, "已过期单位_YYYYMMDD.xlsx")` |
| `#overdue-list` | 列表，**按逾期天数从多到少排**（逾期最久的在最上面） |
| 每行 | 点整行 → 打开单位详情；点右侧「办结」→ 走 `doBanjie(uid)` |

- 判据：`daysUntil(r.validTo) < 0`（`daysUntil` 用当天 0 点做基准，`Math.round((d - t)/86400000)`）。
- 与上方「到期提醒」里那个**已过期分组**是同一批数据，**两处都显示**（用户明确要求保留上方分组）。
  区别：上方受时间窗约束，这里是**全量列出**，便于集中清理。
- 线上实况：791 条底档里有效期止年份分布 `{2026:58, 2027:115, 2028:64, 2029:294, 2030:260}`，
  **当前 0 条已过期**（最早的有效期止就是 2026-09-19）。所以区块默认显示空状态「没有已过期的单位 🎉」。

> ⚠️ 想验证这个区块时，别改真实数据：临时在台账里新增一条 `validTo` 为过去日期的记录，
> 看完立刻删掉并核对总数（`live-verify-v3.js` 第【3】节就是这么做的）。

### 8.2 未编码单位：不上地图，但可以手动在地图上选点

**背景（为什么要有这个）**：未编码单位因为没有坐标 → 不上地图 → 于是**没有任何入口**给它们补坐标，
只能靠「🔄 地理编码全部」按地址批量编码；地址写得不准（如「某小区 3 号楼」）就永远补不上。
所以补了三个手动定位入口，同时**保持「未编码单位不出现在地图上」不变**。

| 入口 | 位置 | 代码 |
|---|---|---|
| ① 「未编码单位」面板 | 地图界面右侧（`#unloc-block`，在「周边单位」下方） | `renderUnlocated()` + `#unloc-list` 点击 → `startManualLocate(uid)` |
| ② 台账「坐标」列的「📍定位」按钮 | 单位台账 → 坐标列（仅未编码行显示） | `data-action="locate"` → `goManualLocate(uid)` |
| ③ 目标卡片「📍 手动定位」按钮 | 地图右侧目标卡片（仅当前目标未编码时显示） | `#tc-locate` → `startManualLocate(uid)` |

流程：点任一个入口 → 进入选点模式（`#amap-container` 加 `.pick-on`，顶部出提示条）→
在地图上点该单位的**实际位置** → `map.on("click")` → `reverseGeocode(lng, lat, rec)`
→ 写入 `lng/lat` + 用真实逆地理编码**回填 `address`** → `commit()` 同步云端 →
该单位立刻变成地图上的点位、并从「未编码单位」面板消失、自动退出选点模式。

- ② 号入口会先 `switchView("map")`，再轮询等 `state.amapReady` 就绪（最多 6 秒）才进选点，
  否则地图还没建好，用户点了也没反应。
- 台账事件委托里 `locate` 分支必须 `e.stopPropagation()`，否则会冒泡成「打开详情」。
- 取消：Esc 键，或再点一次「手动选点」按钮（`exitPickMode()` 会 `renderUnlocated()` 清掉选中高亮）。
- `renderMapView()` 开头就调 `renderUnlocated()`（**不依赖地图是否加载成功**），
  所以高德 Key 没配好、地图加载失败时，这个面板照样能用（会提示「请先配置高德地图密钥」）。

> 💡 剩余未编码单位随时可以在面板里看到数量（`#unloc-count`），
> 目前线上是 **16 条**（791 条中 775 条已编码）。
>
> ⚠️ **这条数字要按「同步完成后」的状态读**（2026-09-19 踩过一次）：解锁后
> `pullCloud` 拉 791 条 + 全量对齐要跑十几秒，这段时间里本地坐标还没被云端填上，
> `#unloc-count` 会显示 **791**（全部未编码，地图上一个点位也没有）。
> 这和「同步完成后真的全部没有坐标」在看板上长得**一模一样**，唯一可靠的区分办法是
> 按时间轴采样：正常情况约 12～13 秒后该数字会从 791 跳到 16、地图同时出现 775 个点位。
> 直接读静态数字而不看时间轴，会把「还没同步完」误判成「数据全坏了」。
>
> 核对权威数字（绕开浏览器，直接查云端）：
> ```bash
> cd .workbuddy-ai/tmp && node unloc.js     # 输出 云端总数 / 已编码 / 未编码 明细
> ```

### 8.3 本地回归 / 真实验证脚本怎么跑

这些脚本放在 `D:\workbuddy\water-309\.workbuddy-ai\tmp\`，而 `.workbuddy-ai/` 是 **gitignore 的**，
所以它们**不在 GitHub 仓库里**，只存在于本机。换机器要重新写。

| 脚本 | 作用 | 跑法 |
|---|---|---|
| `smoke.js` | jsdom 回归测试，**159 项**。改 `workbench/` 下任何文件后先跑它 | `cd .workbuddy-ai/tmp && NODE_PATH="C:/Users/13069/.workbuddy-ai/binaries/node/workspace/node_modules" node smoke.js` |
| `live-verify-v3.js` | 真实无头 Chrome（CDP）对**线上**跑 33 项端到端验证 | `node live-verify-v3.js > livev3.txt 2>&1` |
| `revert-one-unit.js` | 还原 `live-verify-v3.js` 手动定位测试改动过的云端记录 | `node revert-one-unit.js --dry` → `node revert-one-unit.js` |
| `cb-sdk.js` | Node 端直连 CloudBase（绕浏览器 CORS），命令 `whoami\|count\|peek\|probe\|wipe\|push\|fix` | `node cb-sdk.js count` |
| `unloc.js` | 直查云端「未编码」真实条数与明细（权威基线，用来核对页面徽标是否自洽） | `node unloc.js` |
| `rt-verify.js` | 线上实时同步专项验证（6 项：中间态文案 / 已连接 / 791 条 / 断网自动重连 / 数据不丢） | `node rt-verify.js > rtv.txt 2>&1` |

> ⚠️ **`jsdom` 装在受管 Node 目录里**，直接 `node smoke.js` 会报 `Cannot find module 'jsdom'`，
> **必须带上 `NODE_PATH`**（见上表）。
>
> ⚠️ **`live-verify-v3.js` 会真实改动云端一条记录**（第【4】节要验证手动选点，
> 必然要写一次真实坐标 + 走真实高德逆地理编码）。它会把原始值追加到 `revert-targets.json`，
> **跑完务必执行 `node revert-one-unit.js` 还原**，并用 `--check` 核对回到 791 / 16 未编码
> （权威数字用 `node unloc.js` 复核，别只看页面徽标）。
> 同理，第【3】节因为线上 0 条已过期，会临时新增一条 `ZZ-TEST-OD` 记录，脚本自己会删掉并断言总数回到 791。
>
> ⚠️ 真实浏览器验证线上页面时，**必须同时**做到两件事，否则会对着上一版做断言（本项目真踩过）：
> `Network.setCacheDisabled({cacheDisabled:true})` **且** URL 带 `?nc=<时间戳>`
> —— 因为 `index.html` 本身也被 Cloudflare 边缘缓存（`s-maxage=300`）。

---

## 9. 常见问题速查

| 现象 | 原因 | 处理 |
|---|---|---|
| 打开就是锁屏，输密码没反应 | 密码错误 | 密码 `shui309`；错误会提示「密码不正确，还剩 N 次尝试机会」 |
| 输错几次后输入框变灰、按钮点不动 | 连续输错 5 次触发 60 秒冷却（防暴力破解） | 等倒计时结束自动恢复；**刷新页面绕不过去**（计数存 `localStorage`） |
| 用着用着突然要求重新输密码 | 无操作 10 分钟 / 切后台超 1 分钟自动锁定 | 正常行为，重新输入即可（规则见第 2 节） |
| 同一浏览器新开一个标签页也要输密码 | 解锁状态存 `sessionStorage`（按标签页隔离） | 正常行为，这是「关闭标签即失效」的必然代价，见第 2 节 |
| 关掉浏览器再打开要重新输密码 | 同上（这是加固目标，不是 bug） | 正常行为 |
| 云端配置没问题，但一输密码就提示「密码不正确」 | `gate.js` 里的 `KDF_HASH` / `FB_HASH` 改错了 | 用第 2 节的 `node -e` 命令重算，并**务必自测能否登录**（写错就把所有人锁在门外） |
| 顶栏显示「云同步：未配置」 | 环境 ID 没填 | 设置界面填写并保存 |
| 地图整片空白/灰、报红错 | 高德 Key 域名白名单未放行当前域名（如 `water-309.onrender.com`） | 高德后台加白名单，强刷 |
| 地图有底图但没点位 | 这些数据暂无坐标（lng/lat 空） | 地图页点「🔄 地理编码全部」；地址不准的点右侧「未编码单位」里的某一条 → 再点地图上的实际位置手动定位（见 8.2） |
| 首页「已过期单位」是空的 | 当前确实没有已过期单位 | 正常；最早的有效期止是 2026-09-19，到期后会自己出现 |
| 单位列表里有、地图上找不到它 | 该单位没有坐标，不上地图（刻意行为） | 到地图右侧「未编码单位」面板里找到它 → 手动选点（见 8.2） |
| 台账坐标列显示「未编码」 | 同上 | 点旁边的「📍定位」按钮，会自动切到地图并进入选点模式 |
| 导入/新增后保存不了，刷新就没了 | 云集合安全规则没放开写权限 | 按 3.1 第 ④ 步设置 `{"read": true, "write": true}` |
| 页面提示「云端保存失败（数据仅存本地）」 | 同上 | 同上 |
| 顶栏「实时同步：未连接（兜底轮询中）」 | 实时通道没连上（切网 / 休眠 / 凭据用尽） | **数据不受影响**：45 秒兜底轮询仍在同步。正常会自己连回来；若一直不变，刷新页面即可。详见 5.1 |
| 顶栏「实时同步：等待首次同步完成…」 | 刚解锁，正在拉云端全量并对齐 | 正常，十几秒内会变「已连接」 |
| 顶栏下方一直挂着红色「云同步异常」提示条 | 云端配置有问题（集合没建 / 域名没放行 / 权限没开） | 提示条里已写明缺哪一步；修好后**同步成功会自动消失**，也可点「知道了」临时关掉 |
| 同步失败了，本地数据会不会丢 | 不会 | 记录仍在本地，且**下次打开页面会自动补推上云**（见 6.1） |
| 别的设备改了，这边要手动刷新才看得到 | 实时未连上且未到 45 秒轮询 | 等一会，或点「从云端拉取」 |
| 删除单位后刷新又出现 | 旧版同步只 upsert 不删云端 | 已修复（见第 5 节，云端为准） |
| 电脑删了、手机还有 | 删除未传播 | 已修复（云端为准合并）；手机强刷即可 |
| 新设备打开是空的，没有 791 条底档 | 该浏览器本地已有 `sp_shop_data_v2` 键（曾经打开过、或已被清空过） | 种子只在「从未保存过本地数据」时载入，见 6.1 |
| 台账里没有「街道」列 | 打开的是缓存的旧版 `index.html` | Ctrl+Shift+R 强刷；或确认 `index.html` 脚本版本号已升 |
| 云同步失败但顶栏显示「已连接」 | 历史上是安全域名白名单只作用于 `/web`、`/auth` 全局放行所致；现在走 `/cbapi/` 同源代理已不存在 | 用 3.4 节第 ① 条 curl 确认代理是否返回真实 JSON；再查匿名登录（3.1 ③）与集合权限（3.1 ④） |
| 浏览器控制台报 `TypeError: Failed to fetch` | `/cbapi/` 代理没生效（`render.yaml` 的 `routes` 被改 / Render 没同步） | 见 3.4 节，用 curl 验证代理；**不需要**去开通付费的安全域名 |
| 网页端名字不对 | 改名需改 `index.html` 的 `<title>`、`.lock-title`、`.brand` | 三处一起改，UTF-8 |
| 推送 GitHub 报 502 / charset 错误 | HTTPS 走代理不稳定 | 换 SSH：`git remote set-url origin git@github.com:shebbds/water309.git` |
| Blueprint 报「render.yaml not found on main branch」 | 代码还没推到 GitHub（空仓库），或连错了仓库 | 先 `git push -u origin main`；确认连的是 `shebbds/water309` 而非 `shebbds/water-zyf` |
| 新网址打开是旧版页面（旧名字、无密码框） | 打开的是 `water309.onrender.com`（原项目遗留服务） | 本项目网址是 `water-309.onrender.com`（**带连字符**），见 1.6 节 |
| **改了一条记录，过几秒备注自己变回原样，云端也没变** | 旧版存在「并发拉取抹掉刚保存的编辑」的时序 bug（全程无报错） | 已修复（见 5.2 节）。确认线上是 `app.js?v=20260919a` 或更新，然后 Ctrl+Shift+R |
| **别的设备新增的记录，在我这边同步后从云端消失了** | 旧版全量对齐的删除判据过宽 | 已修复（见 5.2 节） |

---

*最后更新：2026-09-19（本轮：① 锁屏说明文字按截图全部删除，门禁四项加固 —— PBKDF2-SHA256 12 万轮校验、连错 5 次冷却 60 秒、无操作 10 分钟 + 切后台 1 分钟自动锁定、解锁状态改存 `sessionStorage`（关闭标签即失效）；② 首页新增独立「已过期单位」区块（上方到期提醒里的已过期分组保留）；③ 未编码单位保持不上地图，新增三个手动在地图上选点补坐标的入口。回归 159 项全绿，真实浏览器 33 项全绿）

*下半场（同日）：④ 实时同步通道加入自建重连 + 新增 `syncing` 中间态文案
（见 5.1 节与「为什么『未连接』可能一直不恢复」）；⑤ 核对权威基线，
确认线上 **791 = 775 已编码 + 16 未编码**（此前文档写的 17 有误）。
回归 168 项全绿，线上实时同步专项 6 项全绿）*

*上轮更新：2026-09-17（本项目 `二次供水309` 从零新建：独立 GitHub 仓库 `shebbds/water309`、独立 Render 服务 `water-309`、网址 `https://water-309.onrender.com`；云同步为腾讯云 CloudBase 文档型数据库，含实时推送与 45 秒兜底轮询；791 条东城区二次供水单位底档；云同步与跨设备编辑已端到端验证通过）*
